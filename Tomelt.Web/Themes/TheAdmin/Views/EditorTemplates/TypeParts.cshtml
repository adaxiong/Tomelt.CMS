@model IEnumerable<Tomelt.ContentTypes.ViewModels.EditTypePartViewModel>
@if (Model.Any()) {
    <div class="easyui-accordion" data-options="multiple:true,selected:''" style="width: 100%;">
        @foreach (var part in Model)
        {
            var p = part;
            @Html.EditorFor(m => p, "TypePart", p.Prefix)
        }
    </div>
}